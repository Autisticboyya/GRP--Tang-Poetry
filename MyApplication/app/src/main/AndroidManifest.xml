<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".homePage"></activity>

        <activity android:name=".personalCenterPage"></activity>

        <activity android:name=".settingPage"> </activity>

        <activity android:name=".gamePage"> </activity>

        <activity android:name=".favouritePage"> </activity>

        <activity android:name=".downloadPage"> </activity>

        <activity android:name=".poemsPage"> </activity>

        <activity android:name=".dailyPoetryPage"> </activity>

        <activity android:name=".userGuidePage"></activity>
        <activity android:name=".appleTree"></activity>
    </application>

</manifest>